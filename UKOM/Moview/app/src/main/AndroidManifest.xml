<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Moview"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Moview.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- UCrop Activity for image cropping -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Cinema Booking Activities -->
        <activity
            android:name=".ui.cinema.MovieScheduleActivity"
            android:theme="@style/Theme.Moview.NoActionBar" />
        <activity
            android:name=".ui.cinema.SeatSelectionActivity"
            android:theme="@style/Theme.Moview.NoActionBar" />
        <activity
            android:name=".ui.cinema.OrderSummaryActivity"
            android:theme="@style/Theme.Moview.NoActionBar" />
        <activity
            android:name=".ui.ticket.TicketHistoryActivity"
            android:theme="@style/Theme.Moview.NoActionBar" />
        <activity
            android:name=".ui.ticket.TicketQrActivity"
            android:theme="@style/Theme.Moview.NoActionBar" />
    </application>

</manifest>